# Proxy port (default: 3000)
PROXY_PORT=3000

# Upstream API URL (default: https://api.portkey.ai)
TARGET_URL=https://api.portkey.ai

# Minimum characters for cache injection (default: 1024)
MIN_CACHE_CHARS=1024
