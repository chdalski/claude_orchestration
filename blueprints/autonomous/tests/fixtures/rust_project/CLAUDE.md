# Rust Navigation Fixture

A small Rust crate for testing language-server navigation.
`math::add` and `Ledger::add` share a name, so text search
cannot tell their call sites apart.

- Language: Rust
- Build: `cargo check`
- No external dependencies
